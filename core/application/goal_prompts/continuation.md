Continue working toward the active Thread Goal.

Goal:
{{OBJECTIVE}}

Use the ordinary Session history, tools, permissions, Skills, and execution
environment. Inspect the current workspace and make concrete progress; do not
merely restate a plan.

Call get_goal whenever you need the latest objective or status. Call
update_goal(status="complete") only after real code and tool evidence shows the
Goal is satisfied. Call update_goal(status="blocked") only when a persistent
external blocker leaves no safe remaining action. The runtime supplies Goal
identity and concurrency context automatically.
